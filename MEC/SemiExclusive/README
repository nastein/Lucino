This folder computes the (e,e'p) cross section for MEC events with one detected proton. It computes dsig/dE'dOmega' dOmegap'dEp'

The probe is along the z axis, the e,e' system is in the x-z plane, and the proton scattering angle is with respect to the beam (z axis). 

The code can be compiled using 'source compile_em.sh'
It can then be run using mpirun -np 8 ./a.out < input_xsec_tot.in

input_tot_xsec.in is the input card which controls the number of events, beam energy, scattering angle, proton scattering angle, proton phi (0 or 180 means in the scattering plane) and CC or EM events
as well as whether you want to use a SF or FG, and things about the Delta propagator

The output file is a list of events, the first line is the total cross section, 
the number of trial events(useful for computing unweighting efficiency), and the number of generated events 
Every event contains the 4 vectors of all the particles involved in order:
incoming lepton
outgoing lepton
incoming nucleon 1
outgoing nucleon 1
incoming nucleon 2
outgoing nucleon 2
weight of the event

If you find that a warning is printed that a weight has been selected which is greater than the max weight
you should unfortunately restart the event generation as this will produced a biased sample
I have only a very simple unweighter implemented, you could change the safety factor
which is the max weight * 2.5 now, you could use a larger safety factor of 5 but the code will be slower
